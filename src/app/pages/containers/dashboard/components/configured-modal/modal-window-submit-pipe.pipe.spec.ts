import { ModalWindowSubmitPipePipe } from './modal-window-submit-pipe.pipe';

describe('ModalWindowSubmitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ModalWindowSubmitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
